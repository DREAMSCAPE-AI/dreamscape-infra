apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
    component: user
    part-of: dreamscape
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
  - name: http
    port: 3002
    targetPort: 3002
    protocol: TCP
